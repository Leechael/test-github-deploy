name: deploy test

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: test
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Phala Cloud
        uses: Leechael/phala-deploy-action@182a5d22f2a2e119cb9f2377ffa5a00ebd64fc29
        with:
          phala-api-key: ${{ secrets.PHALA_CLOUD_API_KEY }}